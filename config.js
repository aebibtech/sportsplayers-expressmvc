// Configuration parameters
const config = {
    PORT: 8000,
    MYSQL_CONFIG: {
        host: 'localhost',
        user: 'root',
        password: '',
        database: 'express_mvc'
    }
};

module.exports = config;